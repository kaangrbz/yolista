# Migrations

Supabase SQL migrations live in **`yolista-web/supabase/migrations/`** (single source of truth).

Apply from the web project:

```bash
cd yolista-web
supabase db push
```

Do not add migration files here.
