/**
 * @deprecated Import from `appThemes` or use `useAppTheme()` instead.
 */
export {
  appTheme,
  APP_THEMES,
  APP_THEME_IDS,
  APP_THEME_LABELS,
  isAppThemeId,
  type AppThemeColors,
  type AppThemeId,
} from './appThemes';
