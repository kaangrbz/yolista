/** Rota oluşturma akışında en fazla fotoğraf / durak sayısı. */
export const MAX_ROUTE_STOPS = 20;

/** Fotoğraf ipucu — DB `routes.title` (StopAdvancedOptions). */
export const STOP_TITLE_MAX_LENGTH = 250;

/** Durak açıklaması — DB `routes.description` (StopForm). */
export const STOP_DESCRIPTION_MAX_LENGTH = 1500;
